# 2026-08-23 の実行で実際に動作していた組み合わせに合わせている。
# supabase は 2.11 未満だと API キーを JWT 形式に限定する検査が入っており、
# 新形式のキー（sb_secret_... / sb_publishable_...）が "Invalid API key" で弾かれる。
pandas==2.3.3
numpy==2.2.6
scikit-learn==1.7.2
supabase==2.31.0
